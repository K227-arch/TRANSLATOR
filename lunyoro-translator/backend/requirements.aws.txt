# AWS-optimized requirements
# ── CPU-only torch (avoids 800MB CUDA wheels — saves ~$0.10/GB ECR storage) ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.1
python-dotenv>=1.0.1

# ── ML inference (kept lean) ──────────────────────────────────────────────────
transformers>=4.41.0,<4.52.0
sentence-transformers>=3.0.0,<4.0.0
sentencepiece>=0.2.0
sacremoses>=0.1.1
huggingface_hub>=0.23.0
optimum[onnxruntime]>=1.18.0
onnxruntime>=1.18.0

# ── Translation utilities ─────────────────────────────────────────────────────
rapidfuzz>=3.9.1
sacrebleu>=2.4.0
numpy>=1.26.0
pandas>=2.2.2
scikit-learn>=1.5.0

# ── File handling ─────────────────────────────────────────────────────────────
openpyxl>=3.1.0
python-docx>=1.1.0
pypdf2>=3.0.1
Pillow>=10.0.0

# ── LLM API client ───────────────────────────────────────────────────────────
openai>=1.0.0
requests>=2.32.0

# ── OCR (optional — comment out to cut 500MB from image if OCR not needed) ───
opencv-python-headless>=4.9.0
easyocr>=1.7.0

# ── Misc ─────────────────────────────────────────────────────────────────────
datasets>=2.19.0
pycryptodome>=3.20.0

# ── AWS SDK ───────────────────────────────────────────────────────────────────
boto3>=1.34.0
